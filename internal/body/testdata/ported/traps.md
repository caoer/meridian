---
type: agent
role: worker
status: blocked
manifest: "parser-trap fixture for mdfs tests"
tags: [type/agent]
---

# Todo
- [x] Reproduce flash failure ^trap-1
- [ ] Capture serial log ^trap-2

# Notes
## Setup
Run the harness like this:

```bash
# this is a comment, not a heading
echo "# also not a heading"
./run.sh --mode=verify
```

> [!warning] Do not power-cycle during readback ^trap-callout

## Log
Serial output captured at 115200 baud.

# Memo
## Setup
Different Setup section under Memo — duplicate name trap.

## Log
Duplicate Log subheading too.

# Q&A: what's next? (v2.1 / "final")
Heading punctuation trap: colon, question mark, parens, slash, quotes.

# Handoff
Successor: check the trap list above.
Last line has no trailing newline