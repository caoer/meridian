---
type: memo
status: draft
tags: [type/memo]
---

# Real
```
# fake heading inside a fence
## another fake heading
### deep fake
plain fence content
```
Body of Real, after the fence.

# AlsoReal
ATX headings inside a fenced code block are content, never section headings. Only `Real`
and `AlsoReal` are sections; the three `#`-lines above are body of `Real`.
